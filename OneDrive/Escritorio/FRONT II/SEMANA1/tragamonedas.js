/*Crear una función que reciba un número que será la cantidad de aciertos del usuario sobre el
 total de casillas disponibles. La función deberá devolver el monto del premio, 
 el cual es proporcional al número de aciertos.*/

/* const prompt = require("prompt-sync")({ sigint: true });
 */

/* const premioTotal = 2500;
const casilla = 10;
	function calcular(aciertos){
 */

/* Crear un programa que consista en una apuesta donde el usuario tenga 
     que ingresar un objeto o lugar con el que soñó ese día, para que luego lo
      muestre en pantalla y se calcula aleatoriamente un resultado numérico con
       4 posibles casos de premios que pueda ganar: si el resultado es 0, gana $1000,
        si es 1, gana $10000; si es 2 gana $10000, y si es 3 gana $100000.
 */

/* function resultadoLoteria() {
  let arreglo = [1000, 10000, 10000, 100000];
  let sueño = prompt("ingrese su sueño aqui!");
  console.log("su sueños es: " + sueño);

  let numero = Math.floor(Math.random() * 3);  //para colocar numeros del 0 al 3 
  console.log(numero);
console.log("su premio es: " + arreglo[numero]);
}
resultadoLoteria(); */

/* Crear una función que sortee un numero random ganador, luego haga un ciclo
desde 1 hasta el final de la ruleta, imprimiendo por consola el número de esa iteración. 
Pero en el caso del número que sea el ganador, imprimir por consola “Numero ganador:” seguido del número.
 */

/* const finalRuleta =  25;

function tirarRuleta(){
    let ruleta = Math.floor(Math.random() * 24) + 1; //math - liberia matematica  - floor permite sacar un numero entero - random selecciona un numero al azar 
console.log(`numero ganador es: ${ruleta}`);
}
console.log(tirarRuleta()); */

/* let tratoperonalizado = confirm("¿desea un trato preferencial?");

if(tratoperonalizado){
    let nombre = prompt ("por favor introduzca si nombre");
    alert("le damos la bienvenida a nuestro sitio" + nombre  + "muchas gracias por visitarnos!!");
}
else {
    alert ("gracias por conectarse");
}
 */

/* alert("esto es una alerta!!")
 */
console.log("esto es una alerta" );